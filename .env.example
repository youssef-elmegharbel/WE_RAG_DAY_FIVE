# Which provider generates answers: gemini | openai | ollama
LLM_PROVIDER=gemini
LLM_MODEL=gemini-2.0-flash

# API keys (only the active provider's key is needed)
GOOGLE_API_KEY=
OPENAI_API_KEY=

# Retrieval tuning
TOP_K=5
SCORE_THRESHOLD=0.35
